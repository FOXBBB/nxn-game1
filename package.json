{
  "name": "nxn-game-root",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd webapp && npm install && npm run build && cd ../backend && npm run build",
    "start": "cd backend && npm run start"
  }
}
